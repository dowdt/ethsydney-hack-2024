import App from "@/components/app";

export default function Home() {
    return (
        // Removed the grid and padding
        <main className="min-h-screen">
            <App />
        </main>
    );
}